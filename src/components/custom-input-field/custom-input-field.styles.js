import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  inputField: {
    marginTop: '17px',
  },
  wide: {
    display: 'flex',
    flexGrow: 1,
    background: '#ffffff',
  },
});

export default useStyles;
